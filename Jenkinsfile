pipeline {
    agent any
    stages {
        stage("build") {
            step {
                echo "Building the app"
            }
        }

        stage("test") {
            step {
                echo "Testing the app"
            }
        }

        stage("deploy") {
            step {
                echo "Deploy the app"
            }
        }
    }
}
